import { createTheme } from "@mui/material/styles";

const kiwi = createTheme({
	palette: {
		mode: "dark",
		primary: {
			main: "#2ed851",
		},
		secondary: {
			main: "#6d4c41",
		},
		info:{
			main:"#6d4c41"
		}
	},
});

export default kiwi;